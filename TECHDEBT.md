# Tech Debt (Lint) — حالة الذمة الموثقة

**الحالة (HEAD cd8630a):** `ruff check .` = **0 — الـ gate أخضر على main.**

> كلّ ما نفّذته حلّه حقيقيّ (يدوي/AST) وصعّدته من baseline-ignored لـ enforced+مُستوفاة — ماشى "ignore" بس.

## ✅ مُحَلّل (30 موقع، 9 عائلات قواعد — مُروّجة للإنفورسمنت)
- **E711/E712** : مُجبرة + `unfixable`. هذه هي الوحيدة اللي كانت تقدر تكسر SQLAlchemy. الآن = 0.
- **UP031** (4): printf → f-string.
- **B007** (2): متغيّر حلقة غير مستخدم.
- **RUF034** (3): if-else فارغ.
- **B904** (6): `raise … from exc` (يحفظ سلسلة الـ traceback — مهم للـ audit الطبي).
- **SIM105** (2): try/except:pass → `contextlib.suppress`.
- **SIM117** (10): `with` متداخل → `with` واحد (نفس ترتيب الدخول/الخروج).
- **PLR1704** (2): إعادة تعريف الوسيط في معالجات الأخطاء → إما إعادة تسمية لـ `exc` أو `except Exception:` bare.
- **RUF043** (1): `match=` نصًا خام (`r'...'`).

## ⏸️ المتبقى (baseline، متعمد تمهيده كـ tech-debt)
تتطلّب إعادة هيكلة أو تعديل سلوك، فما تُصلح بآلة ولا تُقفل بـ collect-only:

### A. إعادة هيكلة/تعقيد (تحتاج تصميم + بوابة تيستات خضراء)
- `TRY300` ~154 — try/except/else.
- `PLR0912` ~70 (فروع كثير)، `PLR0915` ~54 (جمل كثير)، `PLR0911` ~40 (عودات)، `PLR0917` ~27 (positional)، `PLR0913` ~26 (وسائط).
- `RUF012` ~64 — mutable-class-default (dataclass `__init__` shared-mutable → لو غيّرتْها غير محسوب ممكن يكسر).

### B. تغييرات سطح سلوك/معرفات/ملفات (يغيّر واجهة أو IO)
- `PTH123` ~47 — `open()` → pathlib (ملفات — بماكان يلمس IO الطبي).
- `N801/N803/N805/N806/N812/N814/N817` ~50 — إعادة تسمية معرّفات (تأثّر المتصلين).
- `RUF001/002/003` ~43 — أحرف لاتينية غير مُميّزة (**تحذير: فيه نص عربي طبي — ما تُستبدل بالمحرك**).
- `ARG003/004` ~8، `B023` ×2 (late-binding closure — لاصق بيـ Bug مختبأ).
- `PLW0603` ~10 (global — Flask app/db globals)، `PLW1510` ×4 (`subprocess` without `check=True`)، `PLW1508` ×1.
- `F403` ~11 — import-star (متعمّد إتاحته — re-exports؛ يعادل تجاهل F403 في المشروع).

### C. متجاهلة بصمة (ليست "واجبة")
E501، SIM102/SIM103، E402×39، B008، TRY003/004، PTH118/120، PLW2901، ERA001، RUF013/015/100، PLC0415 — تقديريات أسلوب / استيراد دائري / نمط re-export.

## 🚧 حواجز ما يقدر يُدفعش ديون
1. **التيستات المحلية تعلّق (>5 دق، بلا output)** على الملفّات اللي تحتاج PostgreSQL/Stripe/متصفح — والخدمات غير شغّالة في هذه البيئة. البوابة المتاحة = `collect` (1721، 2.1 ث) + تيستات SQLite/mock السريعة.
2. **المجال الطبي**: أي تعديل يُغيّر السلوك (تقسيم دالة، إعادة تسمية، IO، أحرف) يلزم suite أخضر للانزلاق بأمان.

## الخيارات
1. **اقبل الـ baseline كـ tech-debا** (البوابة خضراء؛ هذه هي الحالة الحالية، وفاتورتها هذه الصفحة).
2. **شغّل الخدمات** (PostgreSQL + Stripe mock + browser) عشان الـ full suite يكون أخضر → أدفع العائلات A/B واحدة تلو الأخرى، مختبرة.
