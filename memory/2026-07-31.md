# 2026-07-31

## Work
- Investigating CI failures after seed/tenant-id fixes committed as `231ed04`.
- Unit job (CI) failed `test_create_sale_basic`: `TenantIsolationError: Fail-closed: query on tenant-scoped model Prescription without tenant context in SaaS mode` raised from `tenant_filter_select` (do_orm_execute handler) when `create_sale` ran `select(Prescription).filter(id==x, tenant_id==tid)` with no request tenant context in SaaS mode.
- Could NOT reproduce the unit failure locally: test passes in all configs (create_all DB, migrated DB, SaaS on/off) even WITHOUT the fix. Likely CI-specific (migrated schema / env). Fix still correct as defensive improvement.
- Fix in `app/shared/tenant_filter.py`:
  - Added `_statement_explicitly_scopes_tenant(statement, entity)` helper — detects explicit `tenant_id` predicates in SELECT/WHERE (handles both `Select.whereclause` and legacy `Query._where_criteria`).
  - Exempt query in fail-closed branch of BOTH `tenant_filter_query` (Query.before_compile) and `tenant_filter_select` (do_orm_execute).
  - Did NOT change auto-filter branch (preserves fail-safe double-filter; avoids cross-tenant leak when g.tenant_id differs from explicit tenant_id).
  - Did NOT change `_guard_session_get` (loaded_as_persistent) — stays fail-closed.
- Added regression tests `tests/unit/test_fail_closed_scoped_exempt.py` (3 tests). Verified they fail without the fix, pass with it.
- Ran full `tests/unit/`: 16 passed on both CI-like migrated DB and default local DB.
- flake8 (E9/F63/F7/F82) clean. Pre-existing ruff warnings untouched.
- Committed `934bd98` and pushed to origin/main.

## Discovered: pre-existing entitlement test failure
- `tests/test_saas_seed_and_migration.py::test_migrate_legacy_tenant_creates_line_and_entitlements` fails (also without my fix) in CI `core` job.
- Root cause (hypothesis): `_saas_default_tenant_context` binds the SAAS default test tenant (id=1) to g for this test, but the test migrates a DIFFERENT legacy tenant (id=2). `EntitlementProjectionService.calculate(2)` lazy-loads `SubscriptionLine.entitlement_grants` (selectin, filters by subscription_line_id, NOT tenant_id) → auto-filter appends `tenant_id == 1` (wrong tenant) → empty grants → capabilities missing (e.g. 'schedule') → `is_entitled(2,'schedule')` False.
- NOT caused by my fix; genuine architecture/test-context bug to address separately.

## Known pre-existing CI failures (separate)
- `tests/test_seeds.py::test_local_dev_story_idempotent` — flaky `assert 2 == 1` (sequence/state dependent on local `azad-dev` tenant id).
- `tests/test_saas_seed_and_migration.py::test_migrate_legacy_tenant_creates_line_and_entitlements` — wrong tenant context during projection (above).
